# Core dependencies
torch>=2.0.0
numpy>=1.24.0

# Optional: FAISS for vector search (CPU version)
faiss-cpu>=1.7.4

# Optional: FlashAttention (requires CUDA)
# flash-attn>=2.3.0  # Install separately: pip install flash-attn --no-build-isolation

# Training & distributed training
# deepspeed>=0.12.0  # Optional: for distributed training
# transformers>=4.35.0  # Optional: for tokenizer compatibility
# accelerate>=0.24.0  # Optional: HuggingFace acceleration

# Data processing
# datasets>=2.14.0  # Optional: HuggingFace datasets
# sentencepiece>=0.1.99  # Optional: tokenizer
# webdataset>=0.2.0  # Optional: large-scale data loading

# Serving
# vllm>=0.2.0  # Optional: for vLLM serving backend
# fastapi>=0.104.0  # Optional: API server
# uvicorn>=0.24.0  # Optional: ASGI server

# Development
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
