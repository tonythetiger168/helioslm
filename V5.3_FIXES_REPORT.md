# helioslm v5.3 — Features 對齊修復報告

**日期:** 2026-09-12
**依據:** `helioslm_v5.2_code_review.md`(1 CRITICAL + 10 MAJOR + ~20 MINOR)
**最終狀態:** 測試套件 **19/19 PASS**(exit 0);集成測試 **9/9 PASS**(exit 0);orchestrator 獨立核驗最終磁盤狀態(所有關鍵修復在盤)

---

## 一、CRITICAL 修復

**C1 — Absorbed MLA left-padding NaN 跨層污染**(`mla.py`)
softmax 後 `nan_to_num`,全 mask 行權重歸零,與 SDPA/expanded 行為對齊。驗證:left-padding 下 absorbed 無 NaN,absorbed==expanded diff 2.7e-7;新增回歸測試 `test_mla_absorbed_left_padding`。

## 二、MAJOR 修復(10/10)

### 推理
- **M-I1 MTP 雙重 temperature → 嚴格 speculative sampling**:拒絕位置改從 `norm((p−q)₊)` 重採(標準算法),20 萬次採樣分佈偏差 0.0024 < 3σ(舊路徑偏差 0.208);docstring 更新為嚴格版宣稱。
- **M-I2 `_apply_rope` 形狀陷阱**:`[S,D]→[1,S,1,D]`,B==S 不再靜默錯;非法維度顯式 raise。
- **M-I3 BlockManager 賬簿含 pad 前綴**:按實際 cache 長度(watermark)分配,OOM 會計真實。

### 訓練
- **M-T1 GRPO eval 模式**:train_step 保存/恢復原模式,訓練 forward 前 `model.train()`(finally 保證異常也恢復);dropout>0 時訓練路徑生效已驗證。
- **M-T2 FP8 NaN 守衛**:amax 寫入前 `nan_to_num`;非有限 loss/grad 跳步 + 警告 + 計數,NaN 永不進 master 權重。
- **M-T3 GRPO reward 假陽性**:子串匹配 → 最終答案抽取 + 歸一化相等比較(boxed/最後數字/數值容差);"25" vs "2" 不再誤判。

### 量化 / 文檔
- **M-Q1 GPTQ bias dtype cast**:fp16/bf16 不再崩,回歸測試已加。
- **M-Q2 GPTQ CUDA 兼容**:校准路徑全部張量帶 device。
- **M-Q3 AWQ 校准路徑真修復**:α grid-search(含 α=0 純 RTN 兜底)+ per-group clip search。尖峰激活誤差 **85.1% → 8.76%**(RTN 7.5%);所有分佈下不顯著差於 RTN。
- **M-Q4 README 數字更正**:GPTQ 改為「真實 lite lm_head 激活 10.1%→~7.1% / 合成低秩 10.0%→3.3%」分開標注。

### 模型核心
- **M-C1 mask 單位**:prefill 用 position_ids 作 key positions(packed/非單調正確);decode 非默認 positions 顯式 raise 並文檔化。
- **M-C2 MoE 統計門控**:`expert_load` 僅 training+grad 時累計。
- **M-C3 分布式 bias**:`update_bias` 前 all_reduce(SUM) 全局負載;2-rank gloo 實測各 rank bias 一致。
- **M-C4 RoPE checkpoint**:`persistent=False`,state_dict 不再含 RoPE 表,strict load 正常。

## 三、MINOR 修復(摘要)

GRPO:逐樣本 backward(內存安全,梯度 diff 6e-8)、log-ratio clamp ±20、k3 改 `expm1`、EOS/控制字符清理、契約不明確時顯式 raise。DualPipe:`num_micro_batches` 接線 + 校驗、空輸入守衛、**recompute 保存/恢復 RNG(dropout=0.5 下梯度與直接 fwd+bwd exact 0.0)**、負 id 校驗。FP8:拆雙 history cursor、刪死 buffer、wd/clip 可配、master 跟隨 device。推理:MTP top_p 透傳(model_v5 側 `inspect.signature` 適配 + 不支持時 warn)、`_pad_caches` LRU 上限 64。模型:cos/sin cast 到 x.dtype、config 全字段正數校驗、死字段 batch_size/lr 刪除、**版本字符串全部 v5.3**。

## 四、測試與文檔

- 套件 15→**19 tests**:新增 C1 left-padding 回歸、AWQ 校准質量、.weight property 一致性、MTP 量化後重綁定;修復永真断言;GPTQ RTN 標籤修正。
- README 進版 v5.3(AWQ 校准新描述、GPTQ 數字更正、19 tests);CHANGELOG 追加 v5.3 完整條目。

## 五、驗證證據

| 套件 | 結果 |
|---|---|
| `python -m helioslm_v5.tests.test_v5` | **19/19 PASS**(orchestrator 最終磁盤狀態複跑) |
| `python integration_test_v51.py` | **9/9 PASS** |
| 關鍵修復在盤核驗 | nan_to_num / persistent=False / _residual_sample / top_p 透傳 / bias cast / v5.3 字串 全部 grep 確認 |

## 六、交付物

- `helioslm_v5/`(v5.3 源碼)
- `helioslm_v5.3_full.zip`
- `backups/`:v5.1 / v5.2 / v5.3 三版備份

## 七、剩餘已知限制(如實)

- M-Q2 的 CUDA 路徑僅靜態+CPU 驗證(本機無 GPU);DualPipe CUDA RNG 分支同理。
- decode 時不支援非單調自定義 position_ids(顯式 raise;支援需 cache 存 key positions,契約變更留待將來)。
- fp8_trainer 默認 lr 因死字段清理從 1.5e-4 變為 1e-3(getattr fallback;舊 FP8 checkpoint 因 buffer 更名不嚴格兼容)。
- 嚴格 speculative sampling 已實現,但 top_p tie-breaking 理論上依賴 torch.sort 順序(分佈語義不變)。
