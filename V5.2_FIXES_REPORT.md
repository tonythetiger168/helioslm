# helioslm v5.2 — 遺留功能補齊報告

**日期:** 2026-09-12
**基於:** v5.1(14 CRITICAL + 25 MAJOR 已修復)的 DEFERRED 項與遺留限制
**最終狀態:** 測試套件 **15/15 PASS**(exit 0);集成測試 **9/9 PASS**(exit 0)

---

## 一、本輪補齊的功能(v5.1 遺留 → v5.2 已交付)

### 1. MLA Weight Absorption(v5.1 DEFERRED → FIXED)
- cache 只存共享 latent `(c_kv, k_rope)`;score 用矩陣結合 `(q·W_UK)·c_kv^T + q_rope·k_rope^T`;輸出經 W_UV。decode 每步 O(1) 投影,不再展開整段 cache。
- **真實內存節省:full 配置 576 vs MHA 24,576 元素/token(−97.7%);lite 72 vs 256(−71.9%)**——README 終於可以有依據地寫這個數字。
- 雙模式數值等價:absorbed vs non-absorbed diff 3.3e-07;cached decode vs 全序列 diff 1.8e-07。
- 開關:`config.attention.use_absorption`(預設 True);契約保持(per-layer tuple,dim-2 可截斷)。

### 2. 真 GPTQ Hessian 誤差補償(v5.1 DEFERRED → FIXED)
- `GPTQLinear.from_linear(..., calibration_data=...)`:H = 2XᵀX + percdamp 阻尼,Cholesky 分解,AutoGPTQ 經典 128 列塊循環逐列 OBS 量化 + 誤差補償;group 邊界從已補償權重重算 scale/zero。無校准數據時 RTN fallback(逐位兼容 v5.1)。
- **效果(lite lm_head 真實激活):輸出誤差 10.1% → 3.4%**;相關高斯校準 6.6% → 3.9%。權重空間誤差略高於 RTN 為算法固有(docstring 如實註明)。

### 3. MTP batch>1(v5.1 限制 → FIXED)
- 按 cache 長度分組合批;per-sequence 接受長度;EOS 逐行獨立早停,完成行右填 pad。
- 輸出與逐條 greedy 完全一致(batch=2/3 驗證);`model.generate(use_mtp=True)` 的 batch 限制已解除。

### 4. MTP 回滾:重算前綴 → cache 截斷(FIXED)
- 利用 past dim-2 可切片契約直接截斷,對抗場景下 forward 調用從 12 次降到 8 次;接受路徑零額外開銷。

### 5. 引擎批量步進(v5.1 串行 → FIXED)
- watermark 等長對齊 + pad-token cache 前綴;decode 合批為單次 [B,1] forward(實測 3 條混合 prompt:10 forwards vs 串行 18);輸出 == 逐條 greedy;BlockManager 無洩漏。
- 關鍵坑已處理:pad query 全掩碼行的 NaN 污染問題(兩段式 prefill)。

### 6. Audio 滑窗 memory(v5.1 無界增長 → FIXED)
- `audio_max_memory_frames`(默認 1000),超窗從最舊側截斷;窗內與一次性 forward 數值一致(diff 1.8e-06)。

### 7. 量化兼容性修復(本輪集成發現的真 bug → FIXED)
- AWQLinear/GPTQLinear/FP8Linear 增加 `.weight` property(dequant slow path),修復量化模型 + MLA absorption 的 AttributeError。
- quantize_model 末尾重新綁定 MTP 的 lm_head/embed_tokens 共享引用(v5.1 遺留)。

## 二、驗證證據

| 套件 | 結果 |
|---|---|
| `python -m helioslm_v5.tests.test_v5` | **15/15 PASS**(含新增 test_mla_absorption、test_gptq_calibration、test_audio_sliding_window) |
| `python integration_test_v51.py` | **9/9 PASS**(T2 更新為 batch MTP 正向斷言;T7 增加量化後 MTP 重綁定檢查) |

## 三、交付物

| 文件 | 說明 |
|---|---|
| `helioslm_v5/` | v5.2 源碼(README/CHANGELOG 已進版) |
| `helioslm_v5.2_full.zip` | v5.2 打包 |
| `backups/helioslm_v5.1_full_20260912.zip` | v5.1 備份 |
| `backups/helioslm_v5.2_full_20260912.zip` | v5.2 備份 |

## 四、剩餘已知限制(如實,CHANGELOG 同步)

- 量化模型走 absorbed 路徑時 `.weight` 每次 dequant(slow path);生產應預算 W_UK/W_UV 或用融合 kernel。
- absorbed 路徑手動 softmax,超長 prefill 略慢於 fused SDPA(正確性無差)。
- 引擎對「更長 prompt 中途加入」退化為多長度組獨立 forward(正確但合批率下降)。
- GPTQ 純 Python 逐列循環,1024 維秒級,僅適合離線量化。
- DualPipe 仍為單進程調度模擬(真多卡 p2p 未實現,文檔已如實標注)。
