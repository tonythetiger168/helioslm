# HeliosLM Phase 2 — Pre-training Dependencies

# Core
torch>=2.1.0
numpy>=1.24.0

# Tokenization
sentencepiece>=0.1.99
transformers>=4.35.0
tokenizers>=0.14.0

# DeepSpeed & Distributed Training
deepspeed>=0.12.0
accelerate>=0.24.0

# Data Processing
datasets>=2.14.0
webdataset>=0.2.0
pyarrow>=12.0.0

# FAISS for vector search
faiss-cpu>=1.7.4
faiss-gpu>=1.7.4  # Optional: for GPU-accelerated search

# FlashAttention (requires CUDA; install separately)
# flash-attn>=2.3.0  # pip install flash-attn --no-build-isolation

# Monitoring & Logging
wandb>=0.15.0
tensorboard>=2.14.0
tqdm>=4.65.0

# Serving (Phase 3)
vllm>=0.2.0
fastapi>=0.104.0
uvicorn>=0.24.0

# Development
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.7.0
bandit>=1.7.0

# Utilities
pyyaml>=6.0
jsonlines>=3.1.0
